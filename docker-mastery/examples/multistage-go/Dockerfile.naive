# The "naive" single-stage build: the whole Go toolchain ships to production.
# Compare its size against the multi-stage Dockerfile — often ~300-800MB vs ~15MB.
FROM golang:1.22-alpine
WORKDIR /app
COPY go.mod .
COPY main.go .
RUN CGO_ENABLED=0 go build -o myapp .
CMD ["./myapp"]
